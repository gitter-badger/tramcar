extends ../layout.jade

block title
  title Categories

block content
  h1 Categories

  table(class='table')
    each category in categories
      tr
        td
          a(href="/categories/#{category.id}/jobs")= category.name
